<UserControl x:Class="WSM.App.Shared.Views.LogViewerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:controls="clr-namespace:WSM.App.Shared.Views.Controls"
             mc:Ignorable="d"
             d:DesignHeight="480"
             d:DesignWidth="900">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <Grid Grid.Row="0" Margin="0,0,0,12">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <StackPanel Grid.Row="0"
                  Orientation="Horizontal"
                  VerticalAlignment="Center"
                  TextElement.FontSize="14"
                  TextElement.FontFamily="{StaticResource AppFontFamily}">
        <TextBlock Text="WSM 操作日志"
                   Margin="0,0,12,0"
                   VerticalAlignment="Center"
                   FontFamily="{StaticResource AppFontMedium}" />

        <ComboBox Margin="0,0,12,0"
                  MinWidth="180"
                  VerticalAlignment="Center"
                  materialDesign:HintAssist.Hint="综合或服务"
                  ItemsSource="{Binding ServiceOptions}"
                  SelectedItem="{Binding SelectedService, Mode=TwoWay}"
                  DisplayMemberPath="DisplayName" />

        <CheckBox Margin="0,0,12,0"
                  VerticalAlignment="Center"
                  VerticalContentAlignment="Center"
                  IsChecked="{Binding IsTracking, Mode=TwoWay}"
                  Content="实时跟踪" />

        <CheckBox Margin="0,0,12,0"
                  VerticalAlignment="Center"
                  VerticalContentAlignment="Center"
                  IsChecked="{Binding IsWrapEnabled, Mode=TwoWay}"
                  Content="自动换行" />

        <TextBlock Text="最大条数"
                   Margin="0,0,6,0"
                   VerticalAlignment="Center"
                   FontFamily="{StaticResource AppFontRegular}" />

        <ComboBox Margin="0,0,12,0"
                  MinWidth="80"
                  VerticalAlignment="Center"
                  ItemsSource="{Binding MaxLineOptions}"
                  SelectedItem="{Binding SelectedMaxLines, Mode=TwoWay}" />

        <StackPanel Orientation="Horizontal"
                    VerticalAlignment="Center">
          <Button Style="{StaticResource MaterialDesignFlatButton}"
                  VerticalAlignment="Center"
                  Command="{Binding OpenLogDirectoryCommand}"
                  ToolTip="在资源管理器中打开日志目录">
            <StackPanel Orientation="Horizontal">
              <materialDesign:PackIcon Kind="FolderOutline" Width="16" Height="16" VerticalAlignment="Center" />
              <TextBlock Margin="6,0,0,0" VerticalAlignment="Center" Text="日志目录" />
            </StackPanel>
          </Button>
          <Button Margin="4,0,0,0"
                  Style="{StaticResource MaterialDesignFlatButton}"
                  VerticalAlignment="Center"
                  Command="{Binding CopyAllCommand}"
                  ToolTip="复制全部日志">
            <StackPanel Orientation="Horizontal">
              <materialDesign:PackIcon Kind="ContentCopy" Width="16" Height="16" VerticalAlignment="Center" />
              <TextBlock Margin="6,0,0,0" VerticalAlignment="Center" Text="复制" />
            </StackPanel>
          </Button>
          <Button Margin="4,0,0,0"
                  Style="{StaticResource MaterialDesignFlatButton}"
                  VerticalAlignment="Center"
                  Command="{Binding RefreshCommand}"
                  ToolTip="刷新日志">
            <StackPanel Orientation="Horizontal">
              <materialDesign:PackIcon Kind="Refresh" Width="16" Height="16" VerticalAlignment="Center" />
              <TextBlock Margin="6,0,0,0" VerticalAlignment="Center" Text="刷新" />
            </StackPanel>
          </Button>
          <Button Margin="4,0,0,0"
                  Style="{StaticResource MaterialDesignFlatButton}"
                  VerticalAlignment="Center"
                  Command="{Binding ClearCommand}"
                  ToolTip="清空日志">
            <StackPanel Orientation="Horizontal">
              <materialDesign:PackIcon Kind="DeleteOutline" Width="16" Height="16" VerticalAlignment="Center" />
              <TextBlock Margin="6,0,0,0" VerticalAlignment="Center" Text="清空" />
            </StackPanel>
          </Button>
        </StackPanel>
      </StackPanel>

      <Grid Grid.Row="1"
            Margin="0,8,0,0"
            VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0"
                   Text="方案："
                   Style="{StaticResource AppStatusBarLabelStyle}" />
        <TextBlock Grid.Column="1"
                   Margin="0,0,24,0"
                   Text="{Binding LogSchemeText}"
                   Style="{StaticResource AppStatusBarValueStyle}" />

        <TextBlock Grid.Column="2"
                   Text="路径："
                   Style="{StaticResource AppStatusBarLabelStyle}" />
        <TextBlock Grid.Column="3"
                   Text="{Binding LogPathText}"
                   Style="{StaticResource AppStatusBarValueStyle}"
                   TextTrimming="CharacterEllipsis"
                   ToolTip="{Binding LogPathText}" />
      </Grid>
    </Grid>

    <controls:ConsoleLogView Grid.Row="1"
                             Text="{Binding DisplayText}"
                             WrapLines="{Binding IsWrapEnabled}"
                             AutoScroll="{Binding IsTracking}" />
  </Grid>
</UserControl>
