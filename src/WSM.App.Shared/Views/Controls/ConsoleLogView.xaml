<UserControl x:Class="WSM.App.Shared.Views.Controls.ConsoleLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="800">

  <UserControl.Resources>
    <!-- 轨道翻页区：透明，仅用于点击轨道翻页 -->
    <Style x:Key="ConsoleScrollTrackRepeatButtonStyle"
           TargetType="{x:Type RepeatButton}">
      <Setter Property="OverridesDefaultStyle" Value="True" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderBrush" Value="Transparent" />
      <Setter Property="Focusable" Value="False" />
      <Setter Property="FocusVisualStyle" Value="{x:Null}" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type RepeatButton}">
            <Border Background="Transparent"
                    BorderBrush="Transparent" />
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- 向上箭头：固定 18×14 -->
    <Style x:Key="ConsoleScrollLineUpButtonStyle"
           TargetType="{x:Type RepeatButton}">
      <Setter Property="OverridesDefaultStyle" Value="True" />
      <Setter Property="Width" Value="14" />
      <Setter Property="Height" Value="18" />
      <Setter Property="Focusable" Value="False" />
      <Setter Property="FocusVisualStyle" Value="{x:Null}" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderBrush" Value="Transparent" />
      <Setter Property="Visibility" Value="Collapsed" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type RepeatButton}">
            <Border x:Name="Root"
                    Background="Transparent"
                    Padding="3,4">
              <Path x:Name="Arrow"
                    Fill="#A0A0A0"
                    Width="8"
                    Height="5"
                    Stretch="Uniform"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Data="M 0,5 L 4,1 L 8,5 Z" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Arrow" Property="Fill" Value="#D4D4D4" />
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Arrow" Property="Fill" Value="#FFFFFF" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- 向下箭头：固定 18×14 -->
    <Style x:Key="ConsoleScrollLineDownButtonStyle"
           TargetType="{x:Type RepeatButton}">
      <Setter Property="OverridesDefaultStyle" Value="True" />
      <Setter Property="Width" Value="14" />
      <Setter Property="Height" Value="18" />
      <Setter Property="Focusable" Value="False" />
      <Setter Property="FocusVisualStyle" Value="{x:Null}" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderBrush" Value="Transparent" />
      <Setter Property="Visibility" Value="Collapsed" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type RepeatButton}">
            <Border x:Name="Root"
                    Background="Transparent"
                    Padding="3,4">
              <Path x:Name="Arrow"
                    Fill="#A0A0A0"
                    Width="8"
                    Height="5"
                    Stretch="Uniform"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Data="M 0,0 L 4,4 L 8,0 Z" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Arrow" Property="Fill" Value="#D4D4D4" />
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Arrow" Property="Fill" Value="#FFFFFF" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="ConsoleVerticalScrollBarStyle"
           TargetType="{x:Type ScrollBar}">
      <Setter Property="Background" Value="#2B2B2B" />
      <Setter Property="FocusVisualStyle" Value="{x:Null}" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type ScrollBar}">
            <Grid x:Name="PART_ScrollBar"
                  Width="4"
                  Background="Transparent">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>

              <RepeatButton x:Name="LineUpButton"
                            Grid.Row="0"
                            Command="ScrollBar.LineUpCommand"
                            Style="{StaticResource ConsoleScrollLineUpButtonStyle}" />

              <Track x:Name="PART_Track"
                     Grid.Row="1"
                     IsDirectionReversed="True">
                <Track.DecreaseRepeatButton>
                  <RepeatButton Command="ScrollBar.PageUpCommand"
                                Style="{StaticResource ConsoleScrollTrackRepeatButtonStyle}" />
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                  <Thumb x:Name="PART_Thumb"
                         Background="#A0A0A0"
                         BorderBrush="#A0A0A0"                        
                         BorderThickness="0"
                         Focusable="False"
                         FocusVisualStyle="{x:Null}"
                         Width="2"
                         Margin="0,2,0,2" />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                  <RepeatButton Command="ScrollBar.PageDownCommand"
                                Style="{StaticResource ConsoleScrollTrackRepeatButtonStyle}" />
                </Track.IncreaseRepeatButton>
              </Track>

              <RepeatButton x:Name="LineDownButton"
                            Grid.Row="2"
                            Command="ScrollBar.LineDownCommand"
                            Style="{StaticResource ConsoleScrollLineDownButtonStyle}" />
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Visibility" Value="Collapsed" />
                <Setter Property="Width" Value="4" />
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="LineUpButton" Property="Visibility" Value="Visible" />
                <Setter TargetName="LineDownButton" Property="Visibility" Value="Visible" />
                <Setter TargetName="PART_Thumb" Property="Width" Value="12" />
                <Setter TargetName="PART_ScrollBar" Property="Width" Value="14" />
                <Setter TargetName="PART_ScrollBar" Property="Background" Value="{StaticResource MaterialDesignDarkSeparatorBackground}" />
              </Trigger>
              <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter TargetName="LineUpButton" Property="Visibility" Value="Visible" />
                <Setter TargetName="LineDownButton" Property="Visibility" Value="Visible" />
                <Setter TargetName="PART_Thumb" Property="Width" Value="12" />
                <Setter TargetName="PART_ScrollBar" Property="Width" Value="14" />
                <Setter TargetName="PART_ScrollBar" Property="Background" Value="{StaticResource MaterialDesignDarkSeparatorBackground}" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </UserControl.Resources>

  <Border Background="#1E1E1E"
          CornerRadius="4"
          Padding="12 2 0 2">
    <TextBox x:Name="LogTextBox"
             Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=OneWay}"
             Padding="0 100 0 124"
             TextChanged="OnLogTextChanged"
             IsReadOnly="True"
             AcceptsReturn="True"
             AcceptsTab="True"
             BorderThickness="0"
             Background="Transparent"
             Foreground="#D4D4D4"
             CaretBrush="#D4D4D4"
             VerticalScrollBarVisibility="Auto"
             HorizontalScrollBarVisibility="Disabled">
      <TextBox.Resources>
        <Style TargetType="{x:Type ScrollBar}"
               BasedOn="{StaticResource ConsoleVerticalScrollBarStyle}" />
      </TextBox.Resources>
      <TextBox.Style>
        <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource AppConsoleLogTextStyle}">
          <Setter Property="TextWrapping" Value="NoWrap" />
          <Setter Property="HorizontalScrollBarVisibility" Value="Auto" />
          <Style.Triggers>
            <DataTrigger Binding="{Binding WrapLines, RelativeSource={RelativeSource AncestorType=UserControl}}"
                         Value="True">
              <Setter Property="TextWrapping" Value="Wrap" />
              <Setter Property="HorizontalScrollBarVisibility" Value="Disabled" />
            </DataTrigger>
          </Style.Triggers>
        </Style>
      </TextBox.Style>
    </TextBox>
  </Border>
</UserControl>
